{
  "buildCommand": "pnpm install && cd packages/core && pnpm run build && cd ../react && pnpm run build && cd ../../website && pnpm run build",
  "outputDirectory": "website/.next",
  "installCommand": "pnpm install --ignore-scripts --legacy-peer-deps",
  "framework": "nextjs"
}
